-- ====================================================
--               변수 선언 및 초기화 / 사용
-- ====================================================

-- ====================================================
-- 변수 선언 및 초기화 :    set @변수명 := 값;
--                     select @변수명 := 값;
-- ====================================================
set @x := 10;
set @y := 20;

select @x, @y, @x+@y;


## sakila 데이터베이스 store_id 개수를 변수로 저장
select count(store_id) from sakila.customer;