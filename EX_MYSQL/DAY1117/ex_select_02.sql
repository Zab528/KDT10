-- ========================================================================
-- 그룹화 : Group by 그룹화_기준_컬럼명
--         select 절에 집계함수 사용 : max()/min()/sum()/mean()..
-- ========================================================================
-- DB 선택
use sakila;

--